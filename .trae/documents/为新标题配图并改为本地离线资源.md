## 图片来源说明
- 现有“新年祝福”图片来自代码里的文本生成图片接口：`src/data/defaultCards.ts:13,19,25,31,37` 使用 `.../text_to_image?prompt=...`。
- 为保证线下可用与零网络，我们将改为本地静态资源（SVG/PNG），不再依赖外部接口。

## 要做的事
1. 本地资源改造（离线）
- 在 `src/assets/postcards/` 新增 5 张主题 SVG（或 PNG），并在代码中通过 `import` 引入，所有路径相对且可离线。

2. 更新 5 张明信片的标题与内容
- 所愿皆所得：`新年新气象，所愿皆所得！`
- 更好的自己：`新的一年，遇见更好的自己！`
- 事事皆顺遂：`新年新气象，事事皆顺遂！`
- 生活多点甜：`希望你的生活多点甜🍬`
- 平安喜乐：`🍎平安喜乐，逢考必过！`

3. 为每个主题配套插画（SVG 方案）
- 所愿皆所得：礼盒+星愿+烟花的新年夜景；暖黄/节庆红渐变
- 更好的自己：镜子/剪影+旭日光芒；清新蓝绿渐变
- 事事皆顺遂：如意结/飘带+顺风曲线；金色与孔雀蓝
- 生活多点甜：糖果/棒棒糖/甜甜圈；糖果粉与薄荷色
- 平安喜乐：红苹果+金色书卷或奖章；寓意逢考必过

4. 数据绑定
- 修改 `src/data/defaultCards.ts`：更新 5 条 `title/content/image`，`image` 指向本地资源。
- 保持 LocalStorage 机制不变，设置弹窗支持“重置为默认”。

5. 可选增强（不影响MVP）
- 设置弹窗新增“上传本地图片”以替换默认插画（使用 `URL.createObjectURL` 存引用）。

6. 验证
- 预览抽取结果弹窗显示新插画；断网验证图片仍正常显示；重置默认后恢复新 5 图。

请确认上述方案，我将立即落地实现并把 5 张主题插画与数据替换到代码中。